((benchmarkKind . scenario-e2e)
 (max_total . 200ms)
 (target_total . 100ms)
 (regression_budget . 100ms)
 (expected_over_input_budget . 100ms)
 (targetRationale . "The JSON writer boundary uses a 100ms p95 target and equal regression headroom calibrated for end-to-end parser and policy execution.")
 (sampleCount . 20)
 (purpose . "Keep JSON writer ownership and object lifecycle boundaries within the scenario-owned timing gate")
 (rule . "GERBIL-SCHEME-AGENT-POLICY-WRITER-JSON-BOUNDARY-001")
 (feature . "writer-json-boundary")
 (optimizationFocus . "JSON writer field ownership and object lifecycle boundaries")
 (inputShape . "one report writer mixes object lifecycle, field emission, and JSON serialization responsibilities")
 (expectedOutcome . "separate JSON object lifecycle from field serialization and keep writer ownership explicit")
 (measurementPhases "collect-before" "collect-after" "policy-before" "policy-after" "assert-time-gate" )
 (tags "style" "gerbil-native" "writer" "json" "boundary"))
